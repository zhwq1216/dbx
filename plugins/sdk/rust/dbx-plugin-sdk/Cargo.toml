[package]
name = "dbx-plugin-sdk"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Sidecar protocol SDK for DBX plugins"

[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
